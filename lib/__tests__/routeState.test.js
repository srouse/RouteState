const assert = require('assert');
const routeState = require('index.js');

describe('routeState', function () {
  it('has a test', function () {
    assert(false, 'routeState should have a test');
  });
});
